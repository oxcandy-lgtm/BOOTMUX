arch: aarch64
cpus: 2
memory: 3GiB
disk: 24GiB
vmType: vz
rosetta:
  enabled: false
images:
  - location: "https://cloud-images.ubuntu.com/jammy/current/jammy-server-cloudimg-arm64.img"
    arch: "aarch64"
mounts: []
portForwards:
  - guestPort: 8765
    hostPort: 8765
    hostIP: 127.0.0.1
provision:
  - mode: system
    script: |
      set -eu
      apt-get update
      DEBIAN_FRONTEND=noninteractive apt-get install -y ca-certificates curl git build-essential golang-go nodejs npm
      rm -rf /var/lib/apt/lists/*
message: "BOOTMUX clean ARM64 demo VM"
